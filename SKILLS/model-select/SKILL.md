---
name: model-select
description: 从候选列表中选择最合适的 Qwen 模型，用于 LLM 调用模型路由。
---

# 模型选择技能

## 目标
- 基于任务复杂度与规模在候选模型中选择最优模型。

## 输入
- 模型候选列表
- 任务元信息（规模、数量、预览）
- 默认模型

## 输出
- JSON：`{"model": "...", "reason": "..."}`

## 规则
- 只允许从候选列表中选择。
- 不确定时返回默认模型。
- 严格输出 JSON。
